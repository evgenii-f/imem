# Base URL of the imem backend (runs on the host). The SPA executes in the
# browser, so this is reached as plain localhost regardless of where the
# frontend is served from. Copy to .env to override the built-in default.
VITE_API_BASE_URL=http://localhost:8000
